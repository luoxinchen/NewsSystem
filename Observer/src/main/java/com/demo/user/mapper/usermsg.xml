<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.demo.user.mapper.usermsg">
 
 	<!-- 根据用户名查找相应用户的所有信息 -->
 	<select id="getUsersMassage" resultType="usersmassage" parameterType="String">
 		select * from users where name=#{0}
 	</select>
 </mapper>