package com.demo.login_logout.mapper;

import com.demo.editor.pojo.Users;
import com.demo.editor.pojo.selectImageDB;

public interface login_logout {
	//登录查询用户账号密码
	Users getUsersName(Users users);
}
