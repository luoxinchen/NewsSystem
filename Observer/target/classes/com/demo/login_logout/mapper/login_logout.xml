<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.login_logout.mapper.login_logout">
	<!-- 用户登录 -->
	<select id="getUsersName" parameterType="users" resultType="users">
		select * from usersaccount where name=#{name}
	</select>
</mapper>